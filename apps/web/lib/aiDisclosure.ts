/**
 * Sprint Q4.5 — EU AI Act art. 13 disclosure string.
 *
 * Surfaced via `<AiDisclosureBadge />` next to every page title that drives
 * AI behaviour (sourcing agent, outreach drafts, reply classification, signals,
 * sequence builder).
 */
export const AI_DISCLOSURE =
  "AI ondersteunde functie — recruiter blijft eindverantwoordelijk.";
