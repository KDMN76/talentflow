{
  "name": "talentflow",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "cd apps/api && npm run dev",
    "dev:web": "cd apps/web && npm run dev",
    "dev:contracts": "cd packages/contracts && npm run dev",
    "dev:all": "concurrently --names contracts,api,web --prefix-colors blue,green,yellow --kill-others-on-fail \"npm:dev:contracts\" \"npm:dev:api\" \"npm:dev:web\"",
    "build:api": "cd apps/api && npm run build",
    "build:web": "cd apps/web && npm run build",
    "build:contracts": "cd packages/contracts && npm run build",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "@types/react": "18.3.28",
    "@types/react-dom": "18.3.7"
  },
  "comment:types-react": "App draait op React 18; Radix-peers (@types/react: *) hesen @types/react@19 op root waardoor de React-19 ReactNode (incl. bigint) botste met de React-18 JSX-runtime in apps/web (providers.tsx). Door @types/react 18.x als directe root-devDep te pinnen wint die de root-dedup en is er één versie in de hele monorepo.",
  "comment:ioredis": "bullmq bundelt z'n eigen ioredis; met losse ^-ranges resolvet `npm install` (Docker-build, geen npm ci) een andere ioredis voor bullmq dan de top-level → dubbele Redis-typedefs → TS2322 in de worker-builds. Eén ioredis-versie forceren dedupet dat (zelfde dual-package-fix als @types/react hierboven).",
  "overrides": {
    "@types/react": "18.3.28",
    "@types/react-dom": "18.3.7",
    "ioredis": "5.3.2"
  }
}
