{
  "name": "@talentflow/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Zod schemas + inferred TS types tussen API en web. Single source of truth voor entity-contracts.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch --preserveWatchOutput",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
